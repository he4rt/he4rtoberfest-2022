function calcNumbers(firstNumber: number, secondNumber: number) {
  return firstNumber * secondNumber;
}

const prod = calcNumbers(10, 3);
const prod2 = calcNumbers(5, 5);

console.log(`produto 1: ${prod} \nproduto 2: ${prod2}`);
